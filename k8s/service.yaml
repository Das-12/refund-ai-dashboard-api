apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: refundai
spec:
  type: ClusterIP
  selector:
    app: dashboard-service  
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000